//1.
保留暂存区内容（不要提交到当前分支），贮藏起来。

基于远程 production 新建 production-pick-... 分支，切换到它，把暂存内容提交并推送。

基于远程 beta 新建 beta-pick-... 分支，切换到它，把相同修改提交并推送。

切回 beta（最新），把 beta-pick-... 合并进 beta，推送 beta。
所有操作都在 pick 分支上提交，不在源分支上提交，
最后对比各个 pick 分支的修改是否一致。

2.如果在某一具体的 pick 分支修改后，也是一样的流程，保留到暂存区，贮藏起来。然后将这些修改同步到其他相关的 pick 分支。
最后对比各个 pick 分支的修改是否一致。

3.在所有 pick 分支的修改都完成后，进行最终的对比，确保各个 pick 分支的修改一致。

4.如果某一源分支，新建多个pick分支，超过1个以上需要压缩成一个pick分支，那么需要将多个pick分支的修改合并成一个pick分支，然后进行对比，确保修改一致。

5.暂存贮藏代码后校验，验证代码有无乱码（包括中文乱码，schema）、编译错误，确保代码质量。