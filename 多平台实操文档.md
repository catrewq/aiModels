# Git Safe Flow 多平台实操文档

> 适用 Panda Admin、HR Admin 两个后台项目。
>
> 统一流程：production → feature →（可选）dev → beta → 构建 dist → 验收 → production。
>
> 两个仓库必须分别执行。一个仓库的分支、dist、页面验证或后端验证，都不能替代另一个仓库的结果。

## 一、统一规则与对象

| 阶段 | 做法 | 提交范围 |
|---|---|---|
| 准备 | 确认当前仓库、origin、production/dev/beta 和工作区 | 不提交 |
| 创建 | 从本仓库最新 origin/production 创建 feature | 不提交 |
| 开发 | 只在 feature 上修改业务 | 业务代码，默认排除 dist |
| 提交 | 检查、暂存、commit、push feature | 业务代码 |
| 整理 | 提测前 squash 为一个业务 commit | 不能含 dist |
| 联调 | 需要时合入 dev | 不能含 dist |
| 提测 | 合入 beta | 不能含 dist |
| 构建 | beta 执行 npm run build:beta | 只能提交 dist |
| 发布 | beta 验收通过后合入 production | 按项目规则发布 |

必须遵守：

- production、beta、dev 不直接写业务代码。
- Panda 和 HR 各自确认路径、远程地址、分支、commit、构建和验收结果。
- 业务提交不能把 dist、无关 spec 或其他未授权改动带进去。
- 分支名称中有 pick、beta 或 release，不等于 commit 已进入目标分支；必须用提交关系验证。
- 本流程停用旧命令：New-Personal、Pick-FromPersonal、Promote-Pick、xyh-personal。

### Git 的源代码原理

VS Code、GitLens、SourceTree、Fork、WebStorm 都是 Git 的界面或调用方，最终都在操作以下对象：

| 对象 | 含义 | 检查 |
|---|---|---|
| Working Tree | 编辑器当前文件 | git diff、git status |
| Index | 下一次 commit 的暂存快照 | git diff --cached |
| .git objects | 本地 commit/tree/blob 历史 | git log、git show |
| Local ref | 本地分支指向的 commit | git branch -vv |
| Remote-tracking ref | 最近一次 fetch 看到的远程状态 | git branch -r、git log origin/beta |
| HEAD | 当前检出的分支或 commit | git status |

修改路径是：

~~~text
编辑文件 → 工作区 → git add → 暂存区 → git commit → 本地历史
→ git push → 远程 feature → merge beta/production
~~~

commit 不是 push，push 不是 merge，merge 不是验收。每一层要单独确认。

SVN 不同：本地是带 .svn 的 working copy，svn update 将中央仓库内容合入工作副本，svn commit 直接提交中央仓库；它没有 Git 的本地 commit 再 push 两段式流程。

## 二、准备：Panda 和 HR 分别确认

Panda：

~~~powershell
cd "D:\项目路径\panda-admin"
git status --short --branch
git remote -v
git branch -a
~~~

HR：

~~~powershell
cd "D:\项目路径\panda-admin-hr"
git status --short --branch
git remote -v
git branch -a
~~~

开始前确认：

1. 当前目录是目标仓库。
2. origin 正确。
3. origin/production、origin/dev、origin/beta 存在，或已确认项目实际分支。
4. 工作区无无关修改、暂存、未跟踪或冲突文件。
5. 不在两个仓库之间复制 dist。

## 三、Git Safe Flow PowerShell 脚本（推荐）

脚本位置：

~~~text
D:\桌面\aiModels\git-safe-flow.ps1
~~~

每个仓库单独导入：

~~~powershell
cd "D:\项目路径\panda-admin"
. "D:\桌面\aiModels\git-safe-flow.ps1"
Start-SafeFlow -FeatureBranch "feature/panda-attendance-snapshot"
~~~

PowerShell 的点号加空格是 dot-sourcing，会把函数加载进当前终端；脚本使用当前目录作为 Git 仓库，不会自动切换项目。

### 1. 创建 feature

~~~powershell
$feature = "feature/attendance-snapshot"
New-Feature -FeatureBranch $feature
~~~

脚本会检查工作区干净、fetch origin --prune、确认 origin/production、确认本地和远程没有同名 feature，然后从 origin/production 创建并切换分支，不会 reset 或删除已有分支。

### 2. 提交业务代码

~~~powershell
Submit-Feature -FeatureBranch $feature -Message "feat: 考勤快照功能"
~~~

默认等价于 DistMode Exclude：

~~~powershell
Submit-Feature -FeatureBranch $feature -Message "feat: 考勤快照功能" -DistMode Exclude
~~~

只有明确要提交 dist-only 内容时才用 Include：

~~~powershell
Submit-Feature -FeatureBranch $feature -Message "build: update dist" -DistMode Include
~~~

脚本会 stash push -u 备份、恢复暂存状态、排除 dist、打印 staged paths、执行 git diff --cached --check、要求输入 COMMIT，然后 commit 和 push。失败时会保留 stash hash。

恢复脚本保留的备份：

~~~powershell
git stash list
git stash show --stat stash@{0}
git stash apply --index stash@{0}
~~~

### 3. squash

~~~powershell
Squash-Feature -FeatureBranch $feature -Message "feat: 考勤快照功能"
~~~

脚本会检查 feature 相对 origin/production 的 commit 和文件路径，禁止 feature 含 dist，创建 refs/backup/git-safe-flow/... 备份引用，执行软 reset、一次新 commit，并用 --force-with-lease 推送。确认词是 SQUASH，不要替换成无保护的 --force。

### 4. 合入 dev、beta

~~~powershell
Promote-Feature -FeatureBranch $feature -Target dev
Promote-Feature -FeatureBranch $feature -Target beta
~~~

每次确认词是 MERGE。脚本检查 feature 已推送、相对 production 只有一个业务 commit、变更不含 dist，然后切换目标分支、pull --ff-only、merge --no-ff、push。

dev 不需要联调时跳过。

### 5. beta 构建 dist

~~~powershell
Build-Beta
~~~

确认词是 DIST。脚本切换最新 beta，执行：

~~~powershell
npm run build:beta
~~~

然后确认所有构建变更都在 dist/，只暂存 dist，检查 staged paths 和 whitespace，再提交并推送 beta。构建产生 dist 以外变更时停止。

### 6. 验收和发布 production

~~~powershell
Get-FlowStatus -FeatureBranch $feature
git status --short --branch
~~~

完成页面、接口、权限、真实后端和浏览器验收后：

~~~powershell
Promote-Feature -FeatureBranch $feature -Target production
~~~

确认词是 PRODUCTION。脚本用 git merge-base --is-ancestor 检查 feature 已进入远程 beta，否则阻止 production 合入。

### 7. 脚本命令速查

| 命令 | 作用 | 保护 |
|---|---|---|
| New-Feature | 从 origin/production 建 feature | 工作区干净、禁止同名覆盖 |
| Submit-Feature | 提交并推送业务代码 | stash 备份、默认排除 dist、COMMIT |
| Squash-Feature | 压成一个业务 commit | backup ref、禁止 dist、SQUASH |
| Promote-Feature | 合入 dev/beta/production | production 必须先进入 beta |
| Build-Beta | 构建并提交 beta dist | 只允许 dist、DIST |
| Get-FlowStatus | 查看分支包含状态 | 不代替浏览器和后端验收 |
| Start-SafeFlow | 打印流程 | 只提示，不自动执行 |

## 四、Git 原生命令完整流程

原生命令不会自动提供脚本的 stash 恢复、dist 排除、单 commit、beta gate 和确认词保护，必须自己核对。

### 1. 检查和创建 feature

~~~powershell
git status --short --branch
git remote -v
git fetch origin --prune
git branch -r
git rev-parse origin/production
git switch -c feature/attendance-snapshot origin/production
git push -u origin feature/attendance-snapshot
~~~

### 2. 提交业务代码

~~~powershell
git status --short
git diff
git diff --cached
git add -A -- . ":(exclude)dist/**"
git diff --cached --name-only
git diff --cached --check
git commit -m "feat: 考勤快照功能"
git push -u origin HEAD
~~~

如果要保护当前工作区：

~~~powershell
git stash push -u -m "manual-backup-before-feature-submit"
git stash list
git stash apply --index stash@{0}
~~~

暂存错误时只取消暂存：

~~~powershell
git restore --staged .
~~~

### 3. 手工 squash

~~~powershell
git fetch origin --prune
git log --oneline origin/production..HEAD
git diff --name-only origin/production...HEAD
$oldHead = (git rev-parse HEAD).Trim()
git update-ref "refs/backup/manual-squash/feature-attendance-snapshot" $oldHead
git reset --soft origin/production
git diff --cached --check
git commit -m "feat: 考勤快照功能"
git push --force-with-lease origin HEAD:feature/attendance-snapshot
~~~

### 4. 合入 dev/beta

~~~powershell
git status --short
git fetch origin --prune
git switch dev
git pull --ff-only origin dev
git merge --no-ff origin/feature/attendance-snapshot -m "merge: feature/attendance-snapshot -> dev"
git push origin dev
~~~

~~~powershell
git switch beta
git pull --ff-only origin beta
git merge --no-ff origin/feature/attendance-snapshot -m "merge: feature/attendance-snapshot -> beta"
git push origin beta
~~~

### 5. beta 只提交 dist

~~~powershell
git switch beta
git pull --ff-only origin beta
npm run build:beta
git status --short
git diff --name-only
git add -- dist
git diff --cached --name-only
git diff --cached --check
git commit -m "build: update beta dist"
git push origin beta
~~~

如果 staged paths 有 dist 以外文件，停止，不要使用 git add -A。

### 6. production gate

~~~powershell
git fetch origin --prune
git merge-base --is-ancestor origin/feature/attendance-snapshot origin/beta
if ($LASTEXITCODE -ne 0) { throw "feature 尚未进入 beta，停止发布" }
git switch production
git pull --ff-only origin production
git merge --no-ff origin/feature/attendance-snapshot -m "merge: feature/attendance-snapshot -> production"
git push origin production
~~~

### 7. 核验

~~~powershell
git status --short --branch
git branch -vv
git log --oneline origin/production..feature/attendance-snapshot
git diff --name-only origin/production...feature/attendance-snapshot
git diff-tree --no-commit-id --name-status -r <commit>
git reflog --date=local
~~~

## 五、VS Code：源代码视角和点击流程

VS Code 的 Source Control 使用机器上的 Git。它能 stage、commit、branch、merge、解决冲突，但不知道本项目的 production gate、dist-only 和双仓库规则。建议 VS Code 看代码、diff、按行暂存、解决冲突；分支推进和 beta/production 推送用脚本或终端。

### 1. 打开仓库

1. File → Open Folder 打开 Panda 或 HR 仓库根目录。
2. Terminal 执行 git status --short --branch。
3. 核对左下角分支、资源管理器根路径、终端当前路径。
4. Panda 和 HR 最好分别打开，不要在 multi-root 中混合提交。

### 2. 在 VS Code 终端运行脚本

~~~powershell
. "D:\桌面\aiModels\git-safe-flow.ps1"
$feature = "feature/attendance-snapshot"
New-Feature -FeatureBranch $feature
Submit-Feature -FeatureBranch $feature -Message "feat: 考勤快照功能"
~~~

后续运行 Squash-Feature、Promote-Feature、Build-Beta。

### 3. 界面与原生命令对应

| VS Code 操作 | 对应命令/对象 |
|---|---|
| Source Control → Changes | git diff，工作区 |
| 文件旁加号 | git add file，进入 Index |
| Diff 中 stage hunk | 只暂存部分修改 |
| Staged Changes 减号 | git restore --staged file |
| 输入 message → Commit | git commit |
| 状态栏创建分支 | git switch -c |
| Checkout branch | git switch |
| Git: Merge Branch | git merge |
| Push | git push |
| Merge Changes | 解决冲突后 git add |

### 4. 误区

- 没有 Changes 只说明工作区干净，不说明已经进入 beta。
- Sync Changes 可能同时 pull 和 push，不适合受保护分支。
- Commit 不等于 Push，Push 不等于 Merge。
- 看到 dist/ 不要直接 Stage All。
- Discard Changes 会丢弃未提交修改，先备份和核对路径。

## 六、GitLens：历史、分支和代码来源

GitLens 是 VS Code 扩展，不是另一套 Git。它适合 Commit Graph、Blame、文件历史、分支比较和提交检查，不能替代安全脚本。

### 1. 打开

1. Extensions 搜索 GitLens 并安装。
2. 打开 GitLens 侧栏。
3. Command Palette（Ctrl+Shift+P）运行 GitLens: Show Commit Graph View。
4. 确认当前仓库、分支、远程分支和 fetch 时间。

### 2. 对应 Git Safe Flow

| 阶段 | GitLens | 脚本/终端仍要做 |
|---|---|---|
| 准备 | Home/Repositories | status、remote、fetch |
| 创建 | Branches 看 production 基点 | New-Feature |
| 开发 | Working Changes、Blame、File History | 修改和 Submit-Feature |
| squash | Commit Graph 看提交数量 | Squash-Feature 和 backup ref |
| beta | Compare branches/Common Base | Promote-Feature |
| 恢复 | Stashes、Reflog、Inspect | 核对 hash 后恢复 |

判断 feature 是否已进入 beta：

~~~powershell
git fetch origin --prune
git merge-base --is-ancestor origin/feature/attendance-snapshot origin/beta
~~~

Blame 只说明某行最后由哪个 commit 修改，不代表它已经进入 beta 或 production。

## 七、SourceTree：图形化 Git 流程

界面名称可能随版本变化，按 Fetch、Branches、Working Copy、Commit、Merge、Push 这些稳定入口操作。

### 1. 准备和创建

1. 打开正确仓库。
2. Fetch origin 并清理 stale remote refs。
3. 展开 origin/production、origin/beta、origin/dev。
4. 检查 Working Copy。
5. 推荐在终端运行脚本创建 feature：

~~~powershell
. "D:\桌面\aiModels\git-safe-flow.ps1"
New-Feature -FeatureBranch "feature/attendance-snapshot"
~~~

如果必须点界面：选最新 origin/production → Branch/Create Branch → 填 feature 名 → checkout → Push 设置 upstream。

### 2. 提交

1. Working Copy 逐文件看 diff。
2. 不要在未检查时点击 Stage All。
3. Stage Selected 或按 hunk 暂存。
4. 输入 message → Commit → Push 当前 feature。
5. 更安全的方式是在 SourceTree 看 diff，在终端执行 Submit-Feature。

### 3. 合并、squash、dist

- squash 用 Squash-Feature，SourceTree 只看 commit graph；不要对共享分支无保护 interactive rebase。
- dev/beta 用 Promote-Feature，SourceTree 检查 merge commit 和路径。
- beta dist 用 Build-Beta，不要 Stage All。
- production 用 Promote-Feature -Target production。
- 完全点界面时：checkout beta → Pull 并确认目标最新 → Merge feature → 选择保留 merge commit（等价 --no-ff）→ 逐文件解决冲突 → Commit → Push。

## 八、Fork：图形化 Git 流程

Fork 可查看提交、分支、stash、reflog、interactive rebase、冲突和按行 stage，但不会自动执行 production gate。

### 1. 准备、创建、提交

1. 打开正确 Panda 或 HR 仓库。
2. Fetch origin。
3. 检查 Working Directory Changes。
4. 右键 origin/production 最新 commit → Create Branch/Checkout Branch。
5. 输入 feature/attendance-snapshot。
6. 修改后逐文件或逐行 stage → Commit → Push 并设置 upstream。

推荐脚本：

~~~powershell
. "D:\桌面\aiModels\git-safe-flow.ps1"
New-Feature -FeatureBranch "feature/attendance-snapshot"
Submit-Feature -FeatureBranch "feature/attendance-snapshot" -Message "feat: 考勤快照功能"
~~~

### 2. 检查和合并

- Commit List：看 feature 相对 production 的 commit 数。
- Branch comparison：确认没有 dist。
- Stashes/Reflog：保留脚本失败时的恢复点。
- Interactive Rebase：只用于未共享历史，已共享 feature 用 backup ref 和 --force-with-lease。
- 合并时 checkout beta → fetch/pull → 右键 feature → Merge into Current Branch → 逐文件解决冲突 → 保留 merge commit → Push。
- 业务合入后在 beta 构建 dist，验收通过后再发布 production。

## 九、WebStorm：IDE 内 Git 流程

WebStorm 的主要入口是 VCS widget、Git 菜单、Commit tool window 和 Git tool window。

### 1. 配置和分支

1. Settings → Version Control → Git，确认 Git executable。
2. 打开目标仓库根目录。
3. 查看 VCS widget 当前分支。
4. View → Tool Windows → Git → Log，确认远程分支。
5. Panda 和 HR 分开打开；多 root 时不要误用 Execute branch operations on all roots。

推荐终端运行：

~~~powershell
. "D:\桌面\aiModels\git-safe-flow.ps1"
New-Feature -FeatureBranch "feature/attendance-snapshot"
~~~

纯 IDE：VCS widget → origin/production → New Branch from Selected → 填名并 checkout → Git → Push。

### 2. 提交、合并、冲突

1. Commit tool window 检查 Changes 和 Diff Preview。
2. 只勾选业务文件，排除 dist 和无关 spec。
3. Commit（Ctrl+K），Push（Ctrl+Shift+K）。
4. 生产流程推荐终端执行：

~~~powershell
Submit-Feature -FeatureBranch "feature/attendance-snapshot" -Message "feat: 考勤快照功能"
Squash-Feature -FeatureBranch "feature/attendance-snapshot" -Message "feat: 考勤快照功能"
~~~

5. checkout beta → Update Project → Git → Merge → 选择 origin/feature/... → 3-way Merge 逐项处理 Current/Incoming/Result → Commit → Push。
6. beta 使用 Build-Beta；验收通过后用 Promote-Feature -Target production。

Local History 只能作为 IDE 本地恢复，不能替代 Git commit、stash 或远程备份。

## 十、SVN：独立的工作副本流程

SVN 没有 origin/production、本地 commit、push 和 Git 分支 ref。以下假设仓库采用 trunk/branches/tags 布局。若当前项目是 Git，不要混用 SVN 命令。

### 1. 基础工作循环

~~~powershell
svn checkout "https://svn.example.com/project/trunk" "D:\项目路径\project"
cd "D:\项目路径\project"
svn update
svn status
svn diff
svn add path\to\new-file.vue
svn status
svn diff
svn commit -m "feat: 考勤快照功能" path\to\file.vue path\to\new-file.vue
~~~

svn update 是从中央仓库同步，svn commit 是直接提交中央仓库，两者不会像 Git push/pull 那样拆成同一套动作。

### 2. feature、beta、production

从 trunk 建 feature：

~~~powershell
svn copy "https://svn.example.com/project/trunk" "https://svn.example.com/project/branches/feature/attendance-snapshot" -m "branch: create attendance snapshot"
svn checkout "https://svn.example.com/project/branches/feature/attendance-snapshot" "D:\项目路径\attendance-feature"
~~~

feature 开发：

~~~powershell
cd "D:\项目路径\attendance-feature"
svn update
svn status
svn diff
svn commit -m "feat: 考勤快照功能"
~~~

合入 beta：

~~~powershell
svn checkout "https://svn.example.com/project/branches/beta" "D:\项目路径\attendance-beta"
cd "D:\项目路径\attendance-beta"
svn update
svn merge --dry-run "https://svn.example.com/project/branches/feature/attendance-snapshot"
svn merge "https://svn.example.com/project/branches/feature/attendance-snapshot"
svn status
svn diff
svn commit -m "merge: attendance-snapshot -> beta"
~~~

实际 merge 要结合仓库的 merge tracking 和目录布局，提交前必须检查 svn status 和 svn diff。

### 3. SVN 的 dist、冲突和撤销

~~~powershell
npm run build:beta
svn status
svn diff --summarize
svn commit -m "build: update beta dist" dist
~~~

若构建产生 dist 以外变更，先停止，不要无选择 svn commit。

冲突：

~~~powershell
svn status
svn resolve --accept working path\to\conflicted-file.vue
svn diff
svn commit -m "fix: resolve merge conflict"
~~~

撤销未提交改动：

~~~powershell
svn revert path\to\file.vue
~~~

svn revert 会丢弃指定工作副本改动，使用前确认路径。

### 4. Git 与 SVN 对照

| Git | SVN | 说明 |
|---|---|---|
| git clone | svn checkout | 获取工作副本 |
| git fetch | 无完全等价项 | svn update 会改工作副本 |
| git status | svn status | 查看变化 |
| git diff | svn diff | 查看差异 |
| git add | svn add | 添加新文件 |
| git commit 本地提交 | svn commit 直接提交中央仓库 | 提交位置不同 |
| git switch -c | svn copy URL branches/... | SVN 分支是仓库路径 |
| git merge | svn merge | SVN 有 mergeinfo |
| git push | 无单独 push | svn commit 已发送服务器 |
| git reflog/stash | 工作副本备份、patch、客户端历史 | 没有完全对应物 |

## 十一、两个仓库的实操顺序

Panda：

~~~powershell
cd "D:\项目路径\panda-admin"
. "D:\桌面\aiModels\git-safe-flow.ps1"
$pandaFeature = "feature/panda-attendance-snapshot"
New-Feature -FeatureBranch $pandaFeature
Submit-Feature -FeatureBranch $pandaFeature -Message "feat: Panda 考勤快照"
Squash-Feature -FeatureBranch $pandaFeature -Message "feat: Panda 考勤快照"
Promote-Feature -FeatureBranch $pandaFeature -Target beta
Build-Beta
Get-FlowStatus -FeatureBranch $pandaFeature
# 完成 Panda beta 页面、接口、权限和浏览器验收后
Promote-Feature -FeatureBranch $pandaFeature -Target production
~~~

HR：

~~~powershell
cd "D:\项目路径\panda-admin-hr"
. "D:\桌面\aiModels\git-safe-flow.ps1"
$hrFeature = "feature/hr-attendance-snapshot"
New-Feature -FeatureBranch $hrFeature
Submit-Feature -FeatureBranch $hrFeature -Message "feat: HR 考勤快照"
Squash-Feature -FeatureBranch $hrFeature -Message "feat: HR 考勤快照"
Promote-Feature -FeatureBranch $hrFeature -Target beta
Build-Beta
Get-FlowStatus -FeatureBranch $hrFeature
# 完成 HR beta 页面、接口、权限、真实后端和浏览器验收后
Promote-Feature -FeatureBranch $hrFeature -Target production
~~~

一个仓库失败时，不能宣称两个平台都完成；分别记录仓库、分支、构建日志和剩余验证。

## 十二、冲突、失败和最终验收

Git 冲突：

~~~powershell
git status
git diff --name-only --diff-filter=U
git add path\to\resolved-file.vue
git diff --cached --check
git commit
git push origin <target-branch>
~~~

放弃本次 merge：

~~~powershell
git merge --abort
~~~

不要全局使用 -X ours 或 -X theirs 代替逐块判断。即使要求“以我的为主”，也只在冲突块保留当前版本，非冲突 incoming 改动仍要合并。

脚本失败时：

1. 记录异常、当前分支和 git status。
2. 保留 stash hash 或 refs/backup/git-safe-flow/...。
3. 用 git reflog 确认提交没有丢失。
4. 必要时 git stash apply --index stash@{0}。
5. 未确认前不要 drop stash、删除 backup ref 或清理工作区。

每个仓库单独记录：

~~~text
项目：
仓库路径：
origin：
当前分支：
feature：
feature 相对 origin/production 的业务 commit 数：
业务变更路径：
是否含 dist：
dev 是否包含 feature：
beta 是否包含 feature：
beta dist 是否由本仓库 npm run build:beta 生成：
静态检查：
页面验收：
接口验收：
权限验收：
真实后端验收：
浏览器验收：
production 是否发布：
~~~

最终命令：

~~~powershell
git status --short --branch
git fetch origin --prune
git log --oneline --decorate -8
git diff --name-only origin/production...origin/feature/attendance-snapshot
git merge-base --is-ancestor origin/feature/attendance-snapshot origin/beta
~~~

最后一条成功只能证明 feature commit 已被 beta 包含，不能替代页面、接口、权限或真实后端验收。

## 十三、官方参考

- VS Code Source Control：https://code.visualstudio.com/docs/sourcecontrol/overview
- GitLens：https://help.gitkraken.com/gitlens/gitlens-home/
- SourceTree merge：https://confluence.atlassian.com/get-started-with-sourcetree/merge-changes-from-one-branch-to-another-git-847359121.html
- Fork：https://www.fork.dev/
- WebStorm Git：https://www.jetbrains.com/help/webstorm/using-git-integration.html
- Apache Subversion Quick Start：https://subversion.apache.org/quick-start
- Subversion Book：https://svnbook.red-bean.com/en/1.8/svn.content.html