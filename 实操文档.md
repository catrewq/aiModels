# Git Safe Flow 实操文档

> 标准流程：production → feature →（可选）dev → beta → 验收 → production
>
> 适用于单个后台项目。所有命令都在当前项目仓库中执行。

## 一、必须遵守的规则

1. production 只用于获取线上正式代码，不能直接在上面开发或提交。
2. 每个功能都必须从最新的 origin/production 创建新的 feature 分支。
3. 业务代码只提交到 feature 分支；开发过程中可以有多个临时 commit。
4. 提交提测前，必须把 feature 相对 production 的业务 commit 压缩成一个。
5. dev 是可选联调分支：需要联调就合入，不需要就跳过。
6. dev 和业务 feature 都不提交 dist。
7. beta 是验收分支：合入业务代码后运行 npm run build:beta，构建产物单独提交。
8. beta 验收通过后，feature 才能合入 production。
9. 不直接在 production、beta、dev 上写业务代码，不绕过脚本直接合并。

## 二、准备工作

~~~powershell
# 进入当前项目目录
cd "D:\项目路径\panda-admin-hr"

# 导入脚本
. "D:\桌面\aiModels\git-safe-flow.ps1"

# 查看脚本动作
Start-SafeFlow -FeatureBranch "feature/attendance-snapshot"
~~~

脚本会操作当前仓库的 origin。执行前确认目录、远程仓库和项目都正确。

## 三、从 production 创建 feature

~~~powershell
$feature = "feature/attendance-snapshot"

New-Feature -FeatureBranch $feature
~~~

脚本会：

- 检查工作区是否干净；
- 获取最新远程引用；
- 强制以 origin/production 为基准；
- 如果本地或远程已有同名分支则停止，不会 reset 或删除；
- 创建并切换到新的 feature 分支。

创建后再开始修改代码。

## 四、开发过程中提交业务代码

~~~powershell
# 修改 src/ 下的业务文件

Submit-Feature -FeatureBranch $feature -Message "feat: 完成考勤快照功能"
~~~

Submit-Feature 默认使用 DistMode Exclude：

- 提交前会保留一个安全 stash，并在提交成功、推送成功后才移除；
- 会暂存业务文件；
- 会排除 dist；
- 会检查 staged diff 的 whitespace；
- 会要求输入 COMMIT 确认；
- 会将 commit 推送到远程 feature 分支。

开发过程中如果有多个阶段，可以多次提交：

~~~powershell
Submit-Feature -FeatureBranch $feature -Message "feat: 增加快照列表"
Submit-Feature -FeatureBranch $feature -Message "fix: 修复快照筛选"
Submit-Feature -FeatureBranch $feature -Message "fix: 调整快照弹窗"
~~~

这些 commit 只是开发过程记录，提测前还要执行 squash。

## 五、提测前压缩 feature commit

~~~powershell
Squash-Feature -FeatureBranch $feature -Message "feat: 考勤快照功能"
~~~

脚本会：

- 对比 origin/production 和 feature；
- 检查 feature 不能包含 dist；
- 展示待压缩的 commit；
- 保留旧提交历史备份；
- 要求输入 SQUASH；
- 将所有业务 commit 压成一个；
- 使用 --force-with-lease 安全更新远程 feature；
- 最后验证 feature 相对 production 只有一个业务 commit。

如果相对 production 本来就只有一个 commit，脚本会提示无需 squash。

## 六、可选合入 dev

需要联调时执行：

~~~powershell
Promote-Feature -FeatureBranch $feature -Target dev
~~~

确认提示中输入：

~~~text
MERGE
~~~

dev 合入要求：

- feature 已经 squash 成一个业务 commit；
- feature 不含 dist；
- 工作区干净；
- 目标分支先快进同步远程；
- 合并后推送到远程 dev。

如果不需要 dev 联调，直接跳到下一节，不需要为了流程补一次 dev 合入。

## 七、合入 beta 并构建 dist

先把业务代码合入 beta：

~~~powershell
Promote-Feature -FeatureBranch $feature -Target beta
~~~

输入：

~~~text
MERGE
~~~

然后在 beta 上构建：

~~~powershell
Build-Beta
~~~

Build-Beta 会：

- 切换到最新 beta；
- 执行 npm run build:beta；
- 检查构建产生的文件是否全部位于 dist/；
- 只暂存 dist/；
- 检查 staged paths 和 whitespace；
- 要求输入 DIST；
- 提交 build: update beta dist 并推送 beta。

如果构建产生了 dist 以外的文件，脚本会停止并保留现场，不能继续提交。

## 八、beta 验收通过后合入 production

测试或业务验收通过后执行：

~~~powershell
Promote-Feature -FeatureBranch $feature -Target production
~~~

脚本会先检查：

- feature 仍然是单个业务 commit；
- feature 已经被远程 beta 包含；
- feature 不携带 dist；
- 当前工作区干净。

然后必须输入：

~~~text
PRODUCTION
~~~

才会合并并推送 production。

如果 feature 没有先进入 beta，production 合入会被脚本阻止。

## 九、查看当前推进状态

~~~powershell
Get-FlowStatus -FeatureBranch $feature
~~~

重点看：

- feature 相对 production 有几个 commit；
- feature 修改了哪些文件；
- dev、beta、production 是否已包含该 feature。

## 十、出现冲突或失败时

- 合并冲突：保留当前冲突现场，解决后检查 git status，不要直接 reset。
- 提交失败：脚本会保留安全 stash，先记录输出的 stash hash，再人工检查。
- squash 后推送失败：脚本保留 refs/backup/git-safe-flow/... 历史备份，不要删除。
- beta 构建产生非 dist 文件：先检查这些文件为什么被修改，确认后再处理。
- 远程分支推进导致 push 失败：重新 fetch，检查远程新增提交，再决定如何合并；不要 force push beta、dev 或 production。
- 工作区有与本功能无关的改动：先单独保存或提交，不要让脚本混入本次 feature。

## 十一、旧命令已停用

以下旧流程不再使用：

~~~text
New-Personal
Pick-FromPersonal
Promote-Pick
xyh-personal
~~~

统一改用：

~~~text
New-Feature
Submit-Feature
Squash-Feature
Promote-Feature
Build-Beta
~~~

## 十二、最短操作清单

~~~powershell
. "D:\桌面\aiModels\git-safe-flow.ps1"

$feature = "feature/attendance-snapshot"

New-Feature -FeatureBranch $feature
# 修改代码
Submit-Feature -FeatureBranch $feature -Message "feat: 考勤快照功能"
# 可以继续修改并多次 Submit-Feature
Squash-Feature -FeatureBranch $feature -Message "feat: 考勤快照功能"

# dev 可选
Promote-Feature -FeatureBranch $feature -Target dev

Promote-Feature -FeatureBranch $feature -Target beta
Build-Beta

# beta 验收通过后执行
Promote-Feature -FeatureBranch $feature -Target production
~~~

