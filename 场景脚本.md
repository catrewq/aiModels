# Git Safe Flow 场景脚本

> 基于 `D:\桌面\考勤快照项目` 中 **xiongyonghua** 的实际提交流程提炼。
> 使用 v2.0 脚本（`New-Personal` → `Submit-Personal` → `Pick-FromPersonal` → `Promote-Pick`）。

---

## 核心流程

```
production（官方，不动）
    │
    └── xyh-personal（个人贮藏分支）
            │
            ├── pick -> beta
            │
            └── pick -> production
```

---

## 场景一：创建个人贮藏分支，开发后合入 beta 和 production（全链路）

**流程**：
1. 从 production 创建 `xyh-personal`
2. 在 `xyh-personal` 上提交代码
3. `Pick-FromPersonal` 自动从 `xyh-personal` 找出相对 production/beta 新增的 commits，cherry-pick 到 pick 分支
4. `Promote-Pick` 将 pick 合入 beta/production

```powershell
# 1. 创建个人贮藏分支
New-Personal

# 2. 在 xyh-personal 上开发，提交
Submit-Personal -Message "feat: 考勤快照功能开发"
Submit-Personal -Message "fix: 修复规则设置bug"
Submit-Personal -Message "fix: 调整弹窗样式"

# 3. 合入 beta
Pick-FromPersonal `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-attendance-snapshot"
Promote-Pick `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-attendance-snapshot"

# 4. beta 验证通过后，合入 production
Pick-FromPersonal `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-attendance-snapshot"
Promote-Pick `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-attendance-snapshot"
```

**要点**：
- 所有代码都在 `xyh-personal` 上开发
- pick 分支从 `xyh-personal` 创建，不是从 beta/production
- 同一个 `xyh-personal` 可以分别合入 beta 和 production

---

## 场景二：同一天内多次提交到 beta（迭代优化）

**流程**：上午提交功能 A，下午优化为功能 B，分别创建 pick 合入 beta。

```powershell
# 创建个人贮藏分支
New-Personal

# 上午：提交功能 A
Submit-Personal -Message "feat: 考勤快照功能（初版）"

# 创建 pick 合入 beta
Pick-FromPersonal `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-v1-attendance-snapshot"
Promote-Pick `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-v1-attendance-snapshot"

# 下午：继续优化
Submit-Personal -Message "feat: 考勤快照功能（优化版）"
Submit-Personal -Message "fix: 修复弹窗显示问题"

# 创建第二个 pick 合入 beta
Pick-FromPersonal `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-v2-attendance-snapshot"
Promote-Pick `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-v2-attendance-snapshot"
```

**要点**：
- 同一个 `xyh-personal` 可以多次创建 pick 分支
- 每个 pick 分支包含 `xyh-personal` 从创建以来的所有提交
- pick 分支命名加版本号区分（v1、v2）

---

## 场景三：Pick 分支带 dist 提交，合入 production

**流程**：在 `xyh-personal` 上开发，pick 到 production 前构建 dist。

```powershell
# 在 xyh-personal 上开发
New-Personal
Submit-Personal -Message "feat: add training management module"

# 创建 production pick
Pick-FromPersonal `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-training"

# 在 pick 分支上构建 dist
git switch xiongyonghuaPrivateCode-production-pick-20260806-training
npm run build:production

# 校验构建产物
$changed = git diff --name-only
$outsideDist = $changed | Where-Object { $_ -notmatch '^dist' }
if ($outsideDist) { throw "构建产生了 dist 以外的修改" }

# 提交 dist
git add dist/
git commit -m "build: training dist for production"
git push origin HEAD

# 合入 production（不构建，因为 dist 已在 pick 上）
Promote-Pick `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-training"
    # 注意：Promote-Pick 不传 BuildScript，因为 dist 已在 pick 上
```

**要点**：
- Dist 可以在 pick 分支上提前构建
- Promote-Pick 默认不构建 dist（pick 分支已包含 dist 提交）
- 多个 pick 可以合并到同一个 production commit

---

## 场景四：xyh-personal 同步 production 最新变更

**流程**：production 有更新后，同步到 `xyh-personal`。

```powershell
# 切到 xyh-personal
git switch xyh-personal

# 拉取最新 production
git fetch origin

# 合并 production 到 xyh-personal
git merge origin/production --no-ff -m "sync: merge production into xyh-personal"

# 推送同步后的 xyh-personal
git push origin xyh-personal
```

**要点**：
- `xyh-personal` 需要定期同步 production 的变更
- 避免后续合入时产生大量冲突
- 同步后 `xyh-personal` 仍然包含自己的提交

---

## 场景五：多个 fix squash 成一个 commit

**流程**：在 `xyh-personal` 上多次 fix，squash 后合入 beta。

```powershell
# 在 xyh-personal 上多次提交 fix
Submit-Personal -Message "fix: 优化实名标签宽度"
Submit-Personal -Message "fix: 调整实名标签对齐"
Submit-Personal -Message "fix: 统一实名标签图片尺寸"
Submit-Personal -Message "fix: 居中显示实名照片"
Submit-Personal -Message "fix: 保持实名标签宽度自适应"

# 创建 pick 分支
Pick-FromPersonal `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-real-name-tag"

# 在 pick 分支上 squash
git switch xiongyonghuaPrivateCode-beta-pick-20260806-real-name-tag
git rebase -i origin/beta
# 在编辑器中将 5 个 commit squash 成 1 个
git push --force-with-lease origin HEAD

# 合入 beta
Promote-Pick `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-real-name-tag"
```

**要点**：
- Squash 在 pick 分支上进行，不影响 `xyh-personal`
- 同一功能的多次 fix 可以 squash 成一个 commit
- 保持 target 分支历史整洁

---

## 场景六：一键全流程（New-Personal → Submit-Personal → Pick-FromPersonal → Promote-Pick）

**流程**：日常提交流程，创建贮藏分支后一键执行。

```powershell
# 1. 创建贮藏分支
New-Personal

# 2. 在 xyh-personal 上改代码...

# 3. 提交
Submit-Personal -Message "feat: 考勤快照功能"

# 4. 创建 pick 并合入 beta
Pick-FromPersonal `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-attendance-snapshot"
Promote-Pick `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-attendance-snapshot"

# 5. 构建 dist（可选）
# Promote-Pick 后手动构建：
git switch beta
npm run build:beta
git add dist/
git commit -m "build: 更新 beta 构建产物"
git push origin beta
```

**要点**：
- 日常开发的核心流程就是这 4 步
- 构建 dist 在 Promote-Pick 之后手动执行
- `Start-SafeFlow` 可以快速启动流程（只做提示，不自动执行）

---

## 场景七：紧急 fix 直接提交到 beta（仍走 pick）

**流程**：紧急 fix 仍然走 pick 流程，不跳过。

```powershell
# 1. 确保在 xyh-personal 上
git switch xyh-personal

# 2. 紧急 fix
# ... 修改代码 ...
Submit-Personal -Message "fix: 优化training资源展示和考试滚动"

# 3. 创建 pick 并合入 beta（不走 pick 不行）
Pick-FromPersonal `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-hotfix-training"
Promote-Pick `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-hotfix-training"

# 4. 构建 dist
git switch beta
npm run build:beta
git add dist/
git commit -m "build: 更新 beta 构建产物"
git push origin beta
```

**要点**：
- 紧急 fix 也走 pick 流程，不跳过
- pick 分支名加 `hotfix` 标识

---

## 场景八：多人协作，xyh-personal 与 production 冲突

**流程**：production 有其他人提交后，`xyh-personal` 同步时冲突。

```powershell
# 1. 切到 xyh-personal
git switch xyh-personal

# 2. 尝试同步 production
git fetch origin
git merge origin/production --no-ff

# 3. 解决冲突
# ... 手动解决冲突文件 ...

# 4. 完成合并
git commit -m "sync: resolve conflicts with production"
git push origin xyh-personal
```

**要点**：
- 定期同步 production 到 `xyh-personal` 可以减少冲突
- 冲突在 `xyh-personal` 上解决，不影响官方分支

---

## 场景九：生产环境发布，合并多个 pick

**流程**：beta 验证通过后，多个 pick 合入 production。

```powershell
# 功能 1: 考勤快照
Pick-FromPersonal `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-attendance-snapshot"
Promote-Pick `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-attendance-snapshot"

# 功能 2: 培训模块
Pick-FromPersonal `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-training"
Promote-Pick `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-training"

# 构建 dist
git switch production
npm run build:production
git add dist/
git commit -m "build: 更新 production 构建产物"
git push origin production
```

**要点**：
- Production 发布通常是批量合并多个 pick
- 每个功能独立创建 pick，独立合入
- Dist 构建在 production 上统一执行

---

## 场景十：构建产物单独提交

**流程**：构建产物不混入业务 commit，单独提交。

```powershell
# 在 pick 分支上构建 dist
git switch xiongyonghuaPrivateCode-beta-pick-20260806-attendance-snapshot
npm run build:beta

# 校验构建产物
$changed = git diff --name-only
$outsideDist = $changed | Where-Object { $_ -notmatch '^dist' }
if ($outsideDist) { throw "构建产生了 dist 以外的修改" }

# 提交 dist
git add dist/
git commit -m "build: beta dist assets"
git push origin HEAD
```

**要点**：
- Dist 提交和业务提交分开
- 构建产物不混入业务 commit
- 校验确保构建产物只在 dist 目录

---

## 场景十一：功能分支从当前代码创建

**流程**：代码已在 `xyh-personal` 上，需要创建 feature 分支。

```powershell
# 从 xyh-personal 创建 feature 分支
git switch -c feature/attendance-snapshot xyh-personal
git push -u origin feature/attendance-snapshot
```

**要点**：
- Feature 分支从 `xyh-personal` 创建
- 适合功能已经开发了一半，需要独立管理的情况

---

## 场景十二：Beta 验证通过后发布 production

**流程**：beta 验证没问题后，从 `xyh-personal` 合入 production。

```powershell
# 1. 合入 production
Pick-FromPersonal `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-attendance-snapshot"
Promote-Pick `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-attendance-snapshot"

# 2. 构建 dist
git switch production
npm run build:production
git add dist/
git commit -m "build: 更新 production 构建产物"
git push origin production

# 3. 验证
git log --oneline -3 origin/production
```

**要点**：
- Beta 验证通过后才合入 production
- Production 发布需要构建 dist
- 验证 production 分支状态

---

## 场景十三：完成后清理 xyh-personal

**流程**：代码已合入 beta/production 后，清理个人贮藏分支。

```powershell
# 删除本地 xyh-personal
git branch -D xyh-personal

# 删除远程 xyh-personal
git push origin --delete xyh-personal
```

**要点**：
- 代码合入后可以删除 `xyh-personal`
- 下次需要时重新 `New-Personal` 从 production 创建
- 保持远程仓库干净

---

## 场景十四：xyh-personal 同步最新 beta

**流程**：beta 有更新后，同步到 `xyh-personal`。

```powershell
# 切到 xyh-personal
git switch xyh-personal

# 拉取最新 beta
git fetch origin

# 合并 beta 到 xyh-personal
git merge origin/beta --no-ff -m "sync: merge beta into xyh-personal"

# 推送
git push origin xyh-personal
```

**要点**：
- `xyh-personal` 可以同步 beta 或 production 的变更
- 同步后 `xyh-personal` 包含：production 基础 + beta 变更 + 自己的提交

---

## 场景十五：多个功能从同一个 xyh-personal 分别合入 beta

**流程**：`xyh-personal` 上有多个功能的代码，分别创建 pick 合入 beta。

```powershell
# 在 xyh-personal 上开发多个功能
New-Personal
Submit-Personal -Message "feat: 考勤快照功能"
Submit-Personal -Message "feat: 添加拍照水印"
Submit-Personal -Message "feat: 修改排班规则"

# 功能 1: 考勤快照
Pick-FromPersonal `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-attendance-snapshot"
Promote-Pick `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-attendance-snapshot"

# 功能 2: 拍照水印
Pick-FromPersonal `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-clock-photo-watermark"
Promote-Pick `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-clock-photo-watermark"

# 功能 3: 排班规则
Pick-FromPersonal `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-scheduling-rules"
Promote-Pick `
    -Target "beta" `
    -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-scheduling-rules"
```

**要点**：
- 同一个 `xyh-personal` 可以创建多个 pick 分支
- 每个 pick 包含 `xyh-personal` 的所有提交
- 合入 beta 时每个 pick 产生独立的 merge commit
- 注意：如果功能之间有依赖，合入顺序要注意

---

## 命名规范总结

### 分支命名

| 分支类型 | 命名规范 | 示例 |
|---------|---------|------|
| 个人贮藏分支 | `xyh-personal` | `xyh-personal` |
| Beta pick | `xiongyonghuaPrivateCode-beta-pick-{日期}-{功能}` | `xiongyonghuaPrivateCode-beta-pick-20260806-attendance-snapshot` |
| Production pick | `xiongyonghuaPrivateCode-production-pick-{日期}-{功能}` | `xiongyonghuaPrivateCode-production-pick-20260806-attendance-snapshot` |
| Hotfix pick | `xiongyonghuaPrivateCode-beta-pick-{日期}-hotfix-{描述}` | `xiongyonghuaPrivateCode-beta-pick-20260806-hotfix-training` |

### Commit 命名

| 类型 | 规范 | 示例 |
|------|------|------|
| 功能 | `feat: <功能名>` | `feat: 考勤快照功能开发` |
| 修复 | `fix: <问题描述>` | `fix: 修复规则设置bug` |
| 构建 | `build: <描述>` | `build: 更新 beta 构建产物` |
| 同步 | `sync: <描述>` | `sync: merge production into xyh-personal` |

---

## 高频工作流速查

### 日常开发（xyh-personal → beta）

```powershell
# 1. 创建/切换到个人贮藏分支
New-Personal

# 2. 开发并提交
Submit-Personal -Message "feat: xxx"

# 3. 创建 pick 并合入 beta
Pick-FromPersonal -Target "beta" -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-xxx"
Promote-Pick -Target "beta" -PickBranch "xiongyonghuaPrivateCode-beta-pick-20260806-xxx"

# 4. 构建 dist
git switch beta
npm run build:beta
git add dist/
git commit -m "build: 更新 beta 构建产物"
git push origin beta
```

### 发布到 production

```powershell
# 1. 创建 production pick
Pick-FromPersonal `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-xxx"

# 2. 合入 production
Promote-Pick `
    -Target "production" `
    -PickBranch "xiongyonghuaPrivateCode-production-pick-20260806-xxx"

# 3. 构建 dist
git switch production
npm run build:production
git add dist/
git commit -m "build: 更新 production 构建产物"
git push origin production
```

### 同步 production 到 xyh-personal

```powershell
git switch xyh-personal
git fetch origin
git merge origin/production --no-ff -m "sync: merge production into xyh-personal"
git push origin xyh-personal
```

### 清理 xyh-personal

```powershell
git switch production
git branch -D xyh-personal
git push origin --delete xyh-personal
```
